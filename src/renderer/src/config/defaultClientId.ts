/**
 * Fill this in with your own Twitch Client ID (dev.twitch.tv/console/apps/create,
 * Client Type = Public) before building an installer to share with other people.
 * Doing so lets StickiChat skip the "register an app" step of onboarding entirely
 * for everyone who uses your build — they only need to authorize their own account.
 *
 * Leave empty to require each user to register their own app and paste the ID in.
 */
export const DEFAULT_CLIENT_ID = 'nh4llralr0l36x36w41c00404llzmh'
