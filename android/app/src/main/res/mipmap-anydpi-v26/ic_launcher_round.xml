<?xml version="1.0" encoding="utf-8"?>
<!--
  The background fills the whole canvas: a launcher may mask this to a circle, a squircle or a
  rounded square, and whatever shape it picks has to land on colour rather than on nothing. Only the
  foreground is inset, into the 66.7% the system guarantees it will not crop.

  The monochrome layer is the same glyph. Themed icons are on by default on this launcher, and
  without this Android derives its own silhouette from the colour layers — which is why the icon
  came out as a flat grey S with none of the letter's shape.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@mipmap/ic_launcher_background" />
    <foreground>
        <inset android:drawable="@mipmap/ic_launcher_foreground" android:inset="16.7%" />
    </foreground>
    <monochrome>
        <inset android:drawable="@mipmap/ic_launcher_foreground" android:inset="16.7%" />
    </monochrome>
</adaptive-icon>
